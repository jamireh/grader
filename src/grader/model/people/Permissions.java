package grader.model.people;

/**
 * Permissions are the set of operations allowed.
 */
public class Permissions
{
	/**
	 * Bit string carrying the flags which represent
	 * the operations allowed.
	 */
	int flags;
}